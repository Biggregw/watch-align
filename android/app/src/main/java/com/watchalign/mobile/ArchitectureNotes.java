package com.watchalign.mobile;

/**
 * Alpha54 architecture contract.
 *
 * One public analysis entry point: WatchAlignCore.
 * UI activities own UI only; model metadata comes from ModelCatalog; model-specific
 * geometry is consumed behind the renderer/QC helpers. Legacy experimental core
 * generations are not part of the supported architecture.
 */
final class ArchitectureNotes { private ArchitectureNotes() {} }
